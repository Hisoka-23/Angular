import { CustomizdstyledirectiveDirective } from './customizdstyledirective.directive';

describe('CustomizdstyledirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomizdstyledirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
